apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-deployment
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.netflix.labels.app}}
  template:
    metadata:
      labels:
        app: {{ .Values.netflix.labels.app}}
    spec:
      containers:
        - name: {{ .Values.netflix.containerName }}
          image: "{{ .Values.image.repositoryNetflix }}:{{ .Values.image.tagNetflix }}"
          ports:
            - containerPort: {{ .Values.netflix.containerPort }}