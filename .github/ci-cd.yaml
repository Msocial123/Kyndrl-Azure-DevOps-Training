# this workflow builds a docker image and pushes it to Docker Hub when code is pushed to main branch
name: CI/CD Pipeline    
on:
  push:
    branches:
      - main
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: muralisocial123/ipl:latest

      - name: Deploy to Kubernetes
        uses: azure/k8s-deploy@v3
        with:
          namespace: murali-ns
          manifests: |
            ./deployment.yaml
            ./svc.yaml
          images: muralisocial123/ipl:latest

# =====================================================

# add a step to run unit tessts beofre building and pushing the docker image
      - name: Run Unit Tests
        run: |
          echo "Running unit tests..."
          # add your test commands here
          # for example, if using npm:
          # npm install
          # npm test

#GiHub Actions workflow with:
# - name: CI/CD Pipeline
# - trigger on push to azure-argo-ga branch
# - job: build-and-deploy
# - runs-on: ubuntu-latest
# - Build and push Docker image to Docker Hub
# - Deploy to Kubernetes cluster using kubectl
name: CI/CD Pipeline
on:
  push:
    branches:
      - azure-argo-ga 
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: muralisocial123/ipl:latest

      - name: Deploy to Kubernetes
        uses: azure/k8s-deploy@v3
        with:
          namespace: murali-ns
          manifests: |
            ./deployment.yaml
            ./svc.yaml
          images: muralisocial123/ipl:latest
